# Быстрый старт

Краткое руководство по запуску проекта.

## 🚀 Быстрый запуск через Docker

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd TextGuardAI

# 2. Создать .env файл
cp env.example .env
# Отредактировать .env при необходимости

# 3. Запустить все сервисы
docker-compose up -d

# 4. Применить миграции БД
docker-compose exec api alembic upgrade head

# 5. Проверить работоспособность
curl http://localhost:8000/health
```

API будет доступно по адресу: http://localhost:8000

Документация: http://localhost:8000/docs

## 📦 Установка через Poetry (для разработки)

```bash
# 1. Установить Poetry (если еще не установлен)
curl -sSL https://install.python-poetry.org | python3 -

# 2. Установить зависимости
poetry install

# 3. Активация окружения
poetry shell

# 4. Настройка окружения
cp env.example .env
# Заполнить переменные окружения

# 5. Запустить PostgreSQL и Redis (через Docker)
docker-compose up -d db redis

# 6. Применить миграции
alembic upgrade head

# 7. Запустить приложение
uvicorn app.main:app --reload
```

## 🔧 Разработка

### Запуск Celery Worker

```bash
# В отдельном терминале
celery -A infrastructure.celery_app worker --loglevel=info
```

### Создание миграций

```bash
# Создать новую миграцию
alembic revision --autogenerate -m "Description"

# Применить миграции
alembic upgrade head
```

### Тестирование API

```bash
# Health check
curl http://localhost:8000/health

# Предсказание токсичности
curl -X POST http://localhost:8000/api/v1/predict \
  -H "Content-Type: application/json" \
  -d '{"text": "This is a test message"}'

# История предсказаний
curl http://localhost:8000/api/v1/predictions
```

## 📝 Основные команды

```bash
# Запуск всех сервисов
docker-compose up -d

# Просмотр логов
docker-compose logs -f api

# Остановка
docker-compose down

# Пересборка образов
docker-compose build

# Запуск тестов
pytest

# Форматирование кода
black .
ruff check .
```

## 🔍 Проверка работоспособности

1. **API**: http://localhost:8000/health
2. **Документация**: http://localhost:8000/docs
3. **База данных**: `docker-compose exec db psql -U nlp_user -d nlp_db`
4. **Redis**: `docker-compose exec redis redis-cli ping`

## ⚠️ Частые проблемы

### Порт уже занят

Измените порт в `.env` файле или docker-compose.yml.

### Ошибка подключения к БД

Убедитесь, что PostgreSQL запущен: `docker-compose ps db`

### Ошибка миграций

Проверьте настройки БД в `.env` и убедитесь, что БД доступна.

## 📚 Дополнительная документация

- [README.md](README.md) - Полная документация
- [ARCHITECTURE.md](ARCHITECTURE.md) - Описание архитектуры

