# Архитектура TextGuard AI

Детальное описание архитектуры проекта для обучения продвинутым концепциям разработки.

## 🎯 Цель проекта

Демонстрация enterprise-уровня архитектуры для ML/LLM инженеров с использованием современных паттернов и практик.

## 📐 Принципы архитектуры

### Clean Architecture

Проект следует принципам Clean Architecture с четким разделением на слои:

```
┌─────────────────────────────────────────────┐
│  Presentation Layer (FastAPI)               │
│  - HTTP endpoints                           │
│  - Request/Response схемы                   │
├─────────────────────────────────────────────┤
│  Application Layer                          │
│  - Use Cases (бизнес-сценарии)              │
│  - Application Services                     │
├─────────────────────────────────────────────┤
│  Domain Layer                               │
│  - Entities (бизнес-сущности)               │
│  - Value Objects                            │
│  - Repository Interfaces                    │
├─────────────────────────────────────────────┤
│  Infrastructure Layer                       │
│  - Database (PostgreSQL)                    │
│  - External Services (HTTP)                 │
│  - Background Tasks (Celery)                │
│  - Repository Implementations               │
└─────────────────────────────────────────────┘
```

**Правило зависимостей**: Внутренние слои не зависят от внешних. Все зависимости направлены внутрь.

## 🏛️ Слои архитектуры

### 1. Domain Layer (`domain/`)

**Назначение**: Содержит бизнес-логику и правила предметной области.

**Компоненты**:
- **Entities** (`entities.py`) - Объекты с уникальной идентичностью
  - `PredictionResult` - Результат предсказания
  - `TextClassification` - Классификация текста
  
- **Value Objects** (`value_objects.py`) - Неизменяемые объекты без идентичности
  - `ToxicityLevel` - Уровень токсичности (Enum)
  - `ModelConfig` - Конфигурация модели

- **Repository Interfaces** (`repositories.py`) - Контракты для работы с данными
  - `IPredictionRepository` - Интерфейс репозитория предсказаний

**Принципы**:
- Не зависит от других слоев
- Содержит только бизнес-логику
- Использует интерфейсы для абстракции

### 2. Application Layer (`application/`)

**Назначение**: Оркестрирует работу domain и infrastructure слоев.

**Компоненты**:
- **Use Cases** (`use_cases.py`) - Конкретные бизнес-сценарии
  - `PredictTextUseCase` - Предсказание токсичности текста
  - `GetPredictionHistoryUseCase` - Получение истории предсказаний

- **Services** (`services.py`) - Сервисы приложения
  - `ModelService` - Работа с ML моделью
  - `TextPreprocessingService` - Предобработка текста

- **Interfaces** (`interfaces.py`) - Интерфейсы для сервисов
  - `IModelService`
  - `ITextPreprocessingService`

**Принципы**:
- Координирует работу domain и infrastructure
- Не содержит бизнес-логику (она в domain)
- Использует интерфейсы из domain

### 3. Infrastructure Layer (`infrastructure/`)

**Назначение**: Реализация технических деталей и интеграций.

**Компоненты**:
- **Database** (`database.py`) - Настройка SQLAlchemy
- **Models** (`models.py`) - ORM модели для PostgreSQL
- **Repositories** (`repositories.py`) - Реализация репозиториев
- **HTTP Clients** (`http_clients.py`) - Клиенты для внешних API
- **Celery** (`celery_app.py`, `tasks.py`) - Фоновые задачи
- **Dependency Injection** (`dependency_injection.py`) - DI контейнер

**Принципы**:
- Реализует интерфейсы из domain
- Содержит технические детали
- Может быть заменен без изменения других слоев

### 4. Presentation Layer (`app/api/`)

**Назначение**: Точка входа для HTTP запросов.

**Компоненты**:
- **Routes** (`routes.py`) - FastAPI endpoints
- **Schemas** (`schemas.py`) - Pydantic схемы (DTOs)
- **Main** (`main.py`) - Инициализация приложения

**Принципы**:
- Тонкий слой для HTTP
- Использует use cases из application layer
- Валидация через Pydantic

## 🔄 Паттерны проектирования

### 1. Repository Pattern

**Где**: `domain/repositories.py`, `infrastructure/repositories.py`

**Зачем**: Абстрагирует доступ к данным, позволяет легко менять БД.

```python
# Интерфейс (Domain Layer)
class IPredictionRepository(ABC):
    async def save(self, prediction: PredictionResult) -> PredictionResult: ...

# Реализация (Infrastructure Layer)
class PredictionRepository(IPredictionRepository):
    def __init__(self, session: AsyncSession): ...
```

### 2. Use Case Pattern

**Где**: `application/use_cases.py`

**Зачем**: Инкапсулирует бизнес-сценарии, делает их переиспользуемыми.

```python
class PredictTextUseCase:
    def __init__(self, model_service, repository): ...
    
    async def execute(self, text: str) -> PredictionResult:
        # Оркестрация бизнес-логики
```

### 3. Dependency Injection

**Где**: `infrastructure/dependency_injection.py`

**Зачем**: Управление зависимостями, упрощение тестирования.

```python
container = Container()
container.model_service()  # Создается автоматически
```

### 4. Factory Pattern

**Где**: `domain/entities.py` (метод `create`)

**Зачем**: Инкапсулирует сложную логику создания объектов.

```python
@classmethod
def create(cls, ...) -> "PredictionResult":
    # Сложная логика создания
```

### 5. Service Layer Pattern

**Где**: `application/services.py`

**Зачем**: Координация работы разных компонентов.

### 6. DTO Pattern (Data Transfer Objects)

**Где**: `app/api/schemas.py`

**Зачем**: Валидация и передача данных между слоями.

```python
class TextInputSchema(BaseModel):
    text: str = Field(..., min_length=1)
```

## 🗄️ Работа с данными

### Database Access Pattern

1. **Async SQLAlchemy** - Неблокирующие запросы
2. **Repository Pattern** - Абстракция доступа
3. **ORM Models** - Маппинг на таблицы
4. **Alembic Migrations** - Версионирование схемы

### Data Flow

```
HTTP Request → Pydantic Schema → Use Case → Repository → ORM → PostgreSQL
                                   ↓
                                Domain Entity
```

## 🔧 Dependency Injection

Используется `dependency-injector` для управления зависимостями:

```python
container = Container()

# Singleton services
model_service = container.model_service()

# Factory для use cases (с сессией БД)
use_case = container.predict_text_use_case(session=session)
```

## 📊 Фоновые задачи (Celery)

**Зачем**: Асинхронная обработка тяжелых операций.

**Паттерны**:
- Task Queue Pattern
- Producer-Consumer Pattern

**Использование**:
```python
from infrastructure.tasks import predict_text_async

# Асинхронное выполнение
result = predict_text_async.delay(text)
```

## 🧪 Тестирование

Архитектура упрощает тестирование:

1. **Unit Tests** - Тестирование изолированных компонентов
2. **Integration Tests** - Тестирование взаимодействия слоев
3. **Mocking** - Легко мокировать через интерфейсы

## 📈 Масштабирование

Архитектура поддерживает масштабирование:

1. **Горизонтальное** - Добавление инстансов API
2. **Вертикальное** - Увеличение ресурсов
3. **Микросервисы** - Каждый слой может стать отдельным сервисом

## 🎓 Изучаемые концепции

1. **Clean Architecture** - Разделение ответственности
2. **DDD** - Domain-Driven Design
3. **SOLID принципы** - Особенно Dependency Inversion
4. **Паттерны проектирования** - Repository, Use Case, Factory
5. **Async/await** - Асинхронное программирование
6. **Dependency Injection** - Управление зависимостями
7. **ORM** - Object-Relational Mapping
8. **Task Queues** - Фоновые задачи

## 🔍 Дальнейшее развитие

Проект можно расширить:

1. **CQRS** - Разделение команд и запросов
2. **Event Sourcing** - Сохранение событий
3. **GraphQL** - Альтернатива REST
4. **gRPC** - Для внутренней коммуникации
5. **Kubernetes** - Оркестрация контейнеров
6. **Monitoring** - Prometheus, Grafana
7. **Distributed Tracing** - OpenTelemetry

